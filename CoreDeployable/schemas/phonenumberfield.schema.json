{
  "$id": "https://schema.kainos.com/phonenumberfield.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PhoneNumberField",
  "description": "Representation of phone number input component.",
  "type": "object",
  "properties": {
    "type": {
      "description": "Unique type identifier.",
      "type": "string",
      "const": "PhoneNumberField"
    },
    "name": {
      "description": "Field name used as mapping reference.",
      "type": "string",
      "pattern": "^[a-zA-Z0-9-_]*$"
    },
    "displayText": {
      "description": "Text to use within the label.",
      "type": "string"
    },
    "labelClasses": {
      "description": "Classes to add to the label.",
      "type": "string"
    },
    "isPageHeading": {
      "description": "Whether the label also acts as the heading for the page.",
      "type": "boolean"
    },
    "validation": {
      "$ref": "https://schema.kainos.com/validation.schema.json"
    }
  },
  "required": ["type", "name", "displayText"],
  "additionalProperties": false
}
