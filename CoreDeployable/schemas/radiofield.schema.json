{
  "$id": "https://schema.kainos.com/radiofield.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RadioField",
  "description": "Representation of radios component.",
  "type": "object",
  "properties": {
    "type": {
      "description": "Unique type identifier.",
      "type": "string",
      "const": "RadioField"
    },
    "name": {
      "description": "Name attribute for the radio items.",
      "type": "string"
    },
    "displayText": {
      "description": "Text to use within the legend.",
      "type": "string"
    },
    "isPageHeading": {
      "description": "Whether the legend also acts as the heading for the page.",
      "type": "boolean"
    },
    "labelClasses": {
      "description": "Classes to add to the label.",
      "type": "string"
    },
    "html": {
      "description": "??",
      "type": "string"
    },
    "validation": {
      "$ref": "https://schema.kainos.com/validation.schema.json"
    },
    "hint": {
      "description": "Text to use within the hint.",
      "type": "string"
    },
    "options":{
      "description": "The radio items within the radios component.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          },
          "text": {
            "type": "string"
          },
          "valueText": {
            "type": "string"
          },
          "hint": {
            "type": "string"
          }
        },
        "required": ["text"]
      }
    }
  },
  "required": ["type", "name", "options"]
}
