{
  "$id": "https://schema.kainos.com/textareafield.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TextAreaField",
  "description": "Representation of textarea input component.",
  "type": "object",
  "properties": {
    "type": {
      "description": "Unique type identifier.",
      "type": "string",
      "const": "TextAreaField"
    },
    "name": {
      "description": "Field name used as mapping reference.",
      "type": "string",
      "pattern": "^[a-zA-Z0-9-_]*$"
    },
    "displayText": {
      "description": "Text to use within the label.",
      "type": "string"
    },
    "isPageHeading": {
      "description": "Whether the label also acts as the heading for the page.",
      "type": "boolean"
    },
    "labelClasses": {
      "description": "Classes to add to the label.",
      "type": "string"
    },
    "validation": {
      "$ref": "https://schema.kainos.com/validation.schema.json"
    },
    "hint": {
      "description": "Can be used to add a hint to a text input component.",
      "type": ["string", "null"]
    },
    "maxLength": {
      "description": "If set, the counter will appear below the text area",
      "type": ["number", "null"]
    }
  },
  "required": ["type", "name"]
}
